No real secrets are included in this project.

If needed, a reviewer can use the default Django SECRET_KEY in settings.py
or replace it with their own.

No API keys or external credentials are required to run this project.